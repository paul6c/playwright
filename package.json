{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "testLocal": "ENV=local npx playwright test --project=chromium",
    "testProd": "ENV=prod npx playwright test",
    "testFileSetupLocal": "cross-env ENV=local npx playwright test -g 'sample env file setup' --project=chromium",
    "testUILocal": "ENV=local npx playwright test --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.8",
    "cross-env": "^10.1.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.3"
  }
}
